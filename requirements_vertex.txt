# =============================================================================
# Requirements for the Vertex AI Docker Container
# Uses PyTorch CPU-only to keep image small (~500MB vs ~5GB with CUDA)
# SpikeDLinear is lightweight enough for CPU fine-tuning
# =============================================================================
--extra-index-url https://download.pytorch.org/whl/cpu
torch
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.10.0
EMD-signal>=1.5.0
matplotlib>=3.7.0
google-cloud-storage>=2.14.0
google-cloud-aiplatform>=1.38.0
